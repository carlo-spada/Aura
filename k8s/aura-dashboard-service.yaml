apiVersion: v1
kind: Service
metadata:
  name: aura-dashboard
  namespace: aura
spec:
  type: ClusterIP
  selector:
    app: aura-dashboard
  ports:
    - name: http
      port: 8501
      targetPort: 8501
