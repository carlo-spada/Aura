version: "3.9"
services:
  api:
    build: ./services/api
    container_name: aura_api
    ports:
      - "8000:8000"
    environment:
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:3000}
      - AURA_DATA_DIR=/app/data
      - CLERK_ISSUER=${CLERK_ISSUER}
      - CLERK_JWKS_URL=${CLERK_JWKS_URL}
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped
    volumes:
      - ./data:/app/data

  # Postgres is optional at this stage; API stubs don't require it yet.
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: aura
  #     POSTGRES_PASSWORD: aura
  #     POSTGRES_DB: aura
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

volumes:
  pgdata:
