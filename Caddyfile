{
  email {$ACME_EMAIL}
}

# API subdomain (e.g., api.aura.carlospada.me)
{$AURA_API_DOMAIN} {
  encode zstd gzip
  reverse_proxy api:8000
}

# Dashboard subdomain (e.g., dashboard.aura.carlospada.me)
{$AURA_DASHBOARD_DOMAIN} {
  encode zstd gzip
  reverse_proxy dashboard:8501
}
